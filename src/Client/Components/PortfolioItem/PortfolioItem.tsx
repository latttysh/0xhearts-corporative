import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const PortfolioItem: FunctionComponent<Props> = (props) => {

  return (
      <div></div>
  );
};

export default PortfolioItem;
